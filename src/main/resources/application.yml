# thymeleaf
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecfpxt
    username: root
    password: 159786
    driver-class-name: com.mysql.jdbc.Driver
    type: org.apache.commons.dbcp2.BasicDataSource
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML5
    encoding: UTF-8
    servlet:
      content-type: text/html
    cache: false  # 关闭缓存 及时刷新
  mvc:  #  静态资源文件
    static-path-pattern: /static/**
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 80
    # 热部署配置 相关
#  devtools:
#    restart:
#      enabled: true  #热部署 生效
#      additional-paths: src/main/java  #热部署监听的目录
#      exclude: static/** ,templates/**  # 排除这些 目录文件 不需要 restart